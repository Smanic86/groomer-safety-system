export default function DashboardPage() {
  return (
    <div>
      <h2 className="text-xl font-semibold">Dashboard</h2>
      <p className="text-gray-500 mt-2">Welcome — pick a section from the menu to get started.</p>
    </div>
  );
}